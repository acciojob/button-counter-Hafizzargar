import React from "react";

function Inc() {
const [count,setcount]=useState(0);
  return (
    <div>
      <p>Button clicked {count} times</p>

      <button
        onclick={() => {
          setcount(count + 1);
        }}
      >
        Click me
      </button>
    </div>
  );
}

export default Inc;
